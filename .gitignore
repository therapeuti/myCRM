# IDEs / editors
# Only ignore items that actually exist in this repository (kept minimal per request)

# Local Python virtual environment
.venv/

# Python bytecode directories
__pycache__/

# Local Python version and local lockfile
.python-version
uv.lock